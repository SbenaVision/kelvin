"""Interactive `kelvin init`. Implemented in PR 2."""

from __future__ import annotations

from pathlib import Path


def run_wizard(cwd: Path) -> int:
    raise NotImplementedError("init wizard arrives in PR 2")
